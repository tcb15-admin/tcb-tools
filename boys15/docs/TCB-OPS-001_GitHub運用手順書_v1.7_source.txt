

東海中央ボーイズ 15期
道具割振りツール
GitHub Pages 運用手順書（版 1.11）

文書番号	TCB-OPS-001
版	1.11
作成日	2026年3月27日
作成者	15期管理担当
最終更新	2026年8月9日

改訂履歴
版	改訂日	改訂内容	改訂者
1.0	2026年3月15日	初版作成	15期管理担当
1.1	2026年3月15日	master.json自動同期フロー追加	15期管理担当
1.2	2026年3月21日	GitHubアカウント情報確定・ドキュメント管理方法追加	15期管理担当
1.3	2026年3月27日	マルチ世代テンプレート方式・build.py運用・16期追加手順	15期管理担当
1.4	2026年4月26日	PASTは実施確定後のみ反映（仕様変更）・配布ファイル名修正・master_defaults_boys16・tcb-print-pdf.js・履歴PDF運用を追記	15期管理担当
1.5	2026年5月6日	Cloudflare 同期をマスタの正とする運用を追記・boys16/master.json はリポジトリに置かない・git 整理（.gitignore・旧ファイル削除）	15期管理担当
1.6	2026年5月10日	リポジトリを Public 化し GitHub Pages を有効化（main・/(root)）。15/16期ツールの正式アクセス URL を §1 に明記	15期管理担当
1.7	2026年5月22日	TOOL_VERSION 1.6.3・持ち帰り調整モード・UI_SIMPLE・ドキュメント版更新（SPEC/MAN 1.5・16期マニュアル 1.6）・SYNC_API_TOKEN ビルド時注入を追記	15期管理担当
1.8	2026年5月22日	PAST を lscore 加算・prio 片側ペナルティ（v1.6.4）・Worker 同期	15期管理担当
1.9	2026年7月5日	現行 TOOL_VERSION 1.7.4 に更新。html2pdf.bundle.min.js を同梱アセット化（build.py がコピー。CDN はフォールバックのみ）。SYNC_API_TOKEN 未注入ビルドは同期無効化＋build.py が警告。交代報告 Web Push 用アセット（tcb-push-mgr.js/css・sw.js・tcb-pwa-install.js/css）を追記	15期管理担当
1.10	2026年7月5日	PDF ライブラリ同梱を bundle 版から個別 3 ファイル（jspdf.umd.min.js・html2canvas.min.js・html2pdf.min.js）に変更（bundle 版が html2canvas / jspdf をグローバル公開せず iPhone で PDF 生成に失敗するため）。未ロード時は tcb-print-pdf.js がローカル→CDN の順で動的ロードして復旧	15期管理担当
1.11	2026年8月9日	16期（boys16）を廃止し関連手順・URL を削除（ビルド対象・リポジトリ・D1 から削除済み。復活しない）。現行 TOOL_VERSION 1.11.1。ビルドは boys15 のみ（python3 template/build.py boys15）。JS/CSS 参照へ ?v=TOOL_VERSION を自動付与（キャッシュバスティング。ブラウザキャッシュ起因の版ずれ対策）。追加アセット（tcb-group-hold・tcb-feedback・tcb-presummary・tcb-today-card の js/css）。スモークテスト手順（npm run smoke）を追加。マニュアルは版 1.14（PDF ファイル名 v1.5 のまま差し替え）	15期管理担当

1. アカウント・リポジトリ情報
項目	内容
GitHubユーザー名	tcb15-admin
リポジトリ名	tcb-tools
リポジトリ可視性	Public（無料の GitHub Pages を利用するため）
GitHub Pages ベース URL	https://tcb15-admin.github.io/tcb-tools/（Settings → Pages でブランチ main・フォルダ /(root) を選び Save 後に有効）
15期ツールURL（GitHub Pages）	https://tcb15-admin.github.io/tcb-tools/boys15/（ブックマーク推奨）
GitHubリポジトリ	https://github.com/tcb15-admin/tcb-tools
認証情報管理	Googleドライブ（TCB-ACC-001参照）
現行 TOOL_VERSION（2026-08-09 時点）	1.11.1（15期のみ。16期は廃止・削除済み）

2. リポジトリ構成
パス	内容
template/tool_template.html	共通テンプレート（プレースホルダ付き。機能の正はここ）
template/config_boys15.json	15期固有設定（チーム名・PW・URL・バージョン・UI_SIMPLE 等）
template/build.py	ビルドスクリプト（Python 3）。template + config → 各 index.html 生成。環境変数 SYNC_API_TOKEN でトークン注入可
template/tcb-print-pdf.js	印刷プレビューから PDF Blob を生成（html2pdf.js 利用）。ビルドで boys15/ にコピーされる
template/jspdf.umd.min.js・html2canvas.min.js・html2pdf.min.js	PDF 生成ライブラリ本体（個別同梱。この順で読み込み。読込失敗時は tcb-print-pdf.js がローカル→CDN の順で動的ロード）。ビルドで boys15/ にコピーされる
template/tcb-sync-api.js	Cloudflare 同期 API 呼び出し。ビルドで boys15/ にコピーされる
template/tcb-swap-mgr.js ほか	機能別アセット（tcb-swap-mgr・tcb-push-mgr・tcb-pwa-install・tcb-group-hold・tcb-feedback・tcb-presummary・tcb-today-card の js/css・sw.js）。ビルドで boys15/ にコピーされる
boys15/index.html	15期ツール本体（ビルドで自動生成。GitHub へはこのファイル名で配置）
boys15/tcb-print-pdf.js	上記と同じくビルド成果物。index.html と必ず同じタイミングでデプロイすること
boys15/tcb-sync-api.js	15期用（Cloudflare 同期クライアント。ビルドでコピー。index.html と同時デプロイ）
boys15/master.json	15期ビルド用（build.py が HTML 内 DEFAULT 埋め込みに参照）。運用中のマスタの正は Cloudflare 同期 API（同期 ON のとき）
boys15/images/	15期道具写真（任意）
boys15/docs/	15期ドキュメント類（本書・仕様書・ユーザーマニュアル）
cloudflare-sync/	Worker + D1 スキーマ・デプロイ手順（README 参照）
tests/smoke.mjs・package.json	Playwright スモークテスト（npm run smoke。改修後の回帰確認用）
（16期は 2026-08-09 に廃止。関連ファイル・データは削除済みで再作成しない）

3. 日常の更新フロー

3-1 マスタデータの更新（最重要）
過去担当回数（PAST）は、ツール画面上の「この内容で実施確定（PAST に反映）」実行後にのみ更新されます。「割振りを実行」だけでは PAST は変わりません。
※ Cloudflare 同期を有効にしている場合：「保存」で Worker（D1）に保存され、他端末は読込時にクラウドから取得します。GitHub への master.json アップロードは必須ではありません（バックアップ用にダウンロードする場合のみ従来手順）。
※ 同期を使わない端末向けに master.json を GitHub に載せる運用にする場合のみ、以下手順2〜5を行ってください。
手順	操作
1	活動日で割振りを実行し、必要なら担当を手直ししたうえで「この内容で実施確定（PAST に反映）」を実行（またはマスタ設定を変更）
2	「保存」をタップ → 同期 ON ならクラウドへ保存。同期 OFF なら master.json が自動ダウンロード
3	（同期 OFF のみ）GitHub の boys15/ を開き「Add file」→「Upload files」
4	（同期 OFF のみ）master.json をアップロード → 「Commit changes」
5	全端末が次回アクセス時に自動で最新マスタを取得（同期 ON はクラウド、OFF は URL または端末保存）

3-2 共通機能の改修（テンプレート更新）
ツールの機能改修は tool_template.html（および必要なら各 tcb-*.js/.css）を更新し、boys15 をビルドしてください。
手順	操作
1	修正 → tool_template.html（必要なら tcb-print-pdf.js / tcb-sync-api.js）を更新
2	GitHub の template/ に反映（push）
3	template/config_boys15.json の TOOL_VERSION を更新（例：1.11.0→1.11.1）。この値が JS/CSS 参照の ?v= にも使われ、更新時にブラウザキャッシュが自動で切り替わる
4	（推奨）環境変数 SYNC_API_TOKEN を設定して build.py を実行。未設定時は config 内プレースホルダのまま
5	リポジトリルートで SYNC_API_TOKEN='…' python3 template/build.py boys15 → boys15/ に index.html・kakunin.html・各 js/css が生成される
6	boys15/ の生成物一式を同じコミットで push（HTML と js/css がずれると動作が壊れる可能性あり）。push 前に npm run smoke で回帰確認を推奨

3-3 世代固有の設定変更
template/config_boys15.json を編集（UI_SIMPLE・SYNC_API_BASE_URL 等）
build.py を実行し、当該世代の index.html（と js）を GitHub にアップロード

3-4 道具写真の追加
boys15/images/ に画像をアップロード（フォルダが無ければ作成）
マスタ設定 → 道具の説明・写真 → 画像 URL 欄に例として以下を入力：
https://raw.githubusercontent.com/tcb15-admin/tcb-tools/main/boys15/images/【ファイル名】
「保存」→ 同期 ON ならクラウドへ。OFF なら master.json を GitHub にアップロード

3-5 15期マニュアル PDF の更新
boys15/docs/TCB-MAN-001_道具割り振りツール_操作マニュアル_v1.5.md を編集
cd boys15/docs/scripts && node capture-screenshots.mjs（スクショ更新時）→ node build-pdf.mjs で PDF 再生成
同じファイル名（…v1.5.pdf）で push し URL を変えない（ブックマーク維持）

4. ドキュメントの管理
ドキュメント類は GitHub リポジトリおよびローカル（iCloud 等）で管理します。アカウント管理台帳のみ Google ドライブ（機密）。
ファイル名	最新版	保管場所
TCB-SPEC-001 仕様書	1.14（ファイル名 v1.5）	boys15/docs/ ＋ ローカル
TCB-MAN-001 ユーザーマニュアル	1.14（PDF ファイル名 v1.5）	boys15/docs/ ＋ ローカル
TCB-OPS-001 GitHub運用手順書	1.11（ファイル名 v1.7）	boys15/docs/ ＋ ローカル
TCB-ACC-001 アカウント管理台帳	v1.0	Google ドライブのみ（機密）

5. 世代の管理
運用対象は 15期（boys15）のみ。16期（boys16）は 2026-08-09 に廃止し、ビルド対象・リポジトリ・Cloudflare D1 のデータを削除済み（個人名を含むため。再作成しない）。
項目	15期
ツール URL（利用者向け）	https://tcb15-admin.github.io/tcb-tools/boys15/
マスタの正（推奨）	Cloudflare 同期 API
UI_SIMPLE（上級機能非表示）	true
ビルド用初期マスタ	boys15/master.json（build.py）
初期パスワード	tcb15
GitHub アップロード	15期管理担当
マスタ設定	担当が端末上で編集

6. トラブルシューティング
症状	対処方法
URL にアクセスできない	GitHub Pages 有効化後 5〜10 分待つ。Settings → Pages で main・/(root) が Save 済みか。リポジトリが Public か確認
最新マスタが反映されない	同期 ON なら「保存」で Cloudflare を確認。同期 OFF なら master.json の GitHub 反映を確認。スマホは再読み込み
PAST が想定と違う	「実施確定」を押した日付・担当が最終状態か確認。試行の「割振りを実行」だけでは PAST は増えない
持ち帰り調整で担当が消えた	前当番が基準にいない道具は引き継ぎ0件になりうる。STEP3 で手動割当。固定担当は維持される
PDF が出ない・共有できない	index.html と同じフォルダに tcb-print-pdf.js・jspdf.umd.min.js・html2canvas.min.js・html2pdf.min.js があるか確認（v1.7.4 から個別同梱。無い場合は CDN フォールバックに頼るため通信環境の影響を受ける）
パスワードを忘れた	マスタ設定の JSON をエクスポートして管理者に相談
写真が表示されない	raw.githubusercontent.com の URL が正しいか確認
HTML を差し替えたが変わらない	v1.10.1 以降は JS/CSS に ?v= が付くため、TOOL_VERSION を上げてビルドしていれば自動で切り替わる。反映まで GitHub Pages のデプロイ（数分）を待つ。それでも変わらなければブラウザのキャッシュをクリア
クラウド同期が動かない	SYNC_API_BASE_URL・トークン設定を確認。index.html と tcb-sync-api.js が同じコミットか。ヘッダの同期状態表示とブラウザの開発者ツールのネットワーク／コンソールエラーを確認

最終更新：2026年8月9日
